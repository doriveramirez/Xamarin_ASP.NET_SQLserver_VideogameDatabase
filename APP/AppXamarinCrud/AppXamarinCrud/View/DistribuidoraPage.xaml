<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:d="http://xamarin.com/schemas/2014/forms/design"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             x:Class="MVVM.View.DistribuidoraPage">
    <ContentPage.Content>
        <ScrollView>
            <StackLayout>
                <Label Text="{Binding Id}"/>
                <Entry Placeholder="Nombre" Text="{Binding Nombre}"/>
                <Entry Placeholder="Número de juegos publicados" Text="{Binding NumeroJuegosPublicados}" Keyboard="Numeric"/>
                <Label Text="Imagen"/>
                <Button Text="Guardar" Command="{Binding GuardarCommand}" HeightRequest="40"/>
                <Button Text="Modificar" Command="{Binding ModificarCommand}" HeightRequest="40"/>
                <Button Text="Eliminar" Command="{Binding EliminarCommand}" HeightRequest="40"/>
                <Button Text="Limpiar" Command="{Binding LimpiarCommand}" HeightRequest="40"/>
                <Button Text="Ir" Command="{Binding IrCommand}" HeightRequest="40"/>
                <Button Text="Back" Command="{Binding VolverCommand}" HeightRequest="40"/>
                <ActivityIndicator IsRunning="{Binding IsBusy}" IsVisible="{Binding IsBusy}"/>
                <ListView x:Name="LvDistribuidora" ItemsSource="{Binding Distribuidoras}">
                    <ListView.ItemTemplate>
                        <DataTemplate>
                            <TextCell Text="{Binding Nombre}" Detail="{Binding NumeroJuegosPublicados}"/>
                        </DataTemplate>
                    </ListView.ItemTemplate>
                </ListView>
            </StackLayout>
        </ScrollView>
    </ContentPage.Content>
</ContentPage>